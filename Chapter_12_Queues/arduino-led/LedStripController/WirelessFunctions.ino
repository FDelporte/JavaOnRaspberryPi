#include <SPI.h>

/*
#include <NRFLite.h>

NRFLite radio;
uint8_t radioData;

void initWireless() {
  radio.init(1, 9, 10); // Set this radio's Id = 1, along with its CE and CSN pins
}

void checkWireless() {
  while (radio.hasData()) {
    radio.readData(&radioData);

    Serial.print("Received from radio: ");
    Serial.println(radioData);
  }
}
*/
